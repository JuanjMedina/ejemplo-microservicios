server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka-service
          uri: https://github.com/JuanjMedina/ejemplo-microservicios
          search-paths: config-data

  application:
    name: config-service
